let all=[]; const grid=document.querySelector('#grid'), search=document.querySelector('#search'), detail=document.querySelector('#detail'), detailBody=document.querySelector('#detailBody');
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
fetch('data/characters.json').then(r=>r.json()).then(d=>{all=d;render(all)}).catch(()=>{grid.innerHTML='<p>Data karakter gagal dimuat.</p>'});
function render(items){grid.innerHTML='';document.querySelector('#empty').style.display=items.length?'none':'block';items.forEach(c=>{const a=document.createElement('article');a.className='card';a.innerHTML=`<img class="portrait" src="${esc(c.image||'images/placeholder.svg')}" alt="${esc(c.name)}"><div class="content"><h2>${esc(c.name)}</h2><div class="role">${esc(c.role||'Karakter')}</div><div class="tags">${[c.group,c.city,c.power].filter(Boolean).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div>`;a.onclick=()=>show(c);grid.appendChild(a)})}
function show(c){detailBody.innerHTML=`<div class="detailGrid"><img src="${esc(c.image||'images/placeholder.svg')}" alt="${esc(c.name)}"><div class="detailText"><h2>${esc(c.name)}</h2><p class="role">${esc(c.role||'')}</p><p class="facts"><b>Umur:</b> ${esc(c.age||'-')}<br><b>Tinggi:</b> ${esc(c.height||'-')}<br><b>Rambut:</b> ${esc(c.hair||'-')}<br><b>Mata:</b> ${esc(c.eyes||'-')}<br><b>Kekuatan/Senjata:</b> ${esc(c.power||'-')}<br><b>Kelompok:</b> ${esc(c.group||'-')}<br><b>Tempat:</b> ${esc(c.city||'-')}</p><p class="bio">${esc(c.description||'Belum ada deskripsi.')}</p></div></div>`;detail.showModal()}
search.addEventListener('input',()=>{const q=search.value.toLowerCase();render(all.filter(c=>Object.values(c).join(' ').toLowerCase().includes(q)))})
